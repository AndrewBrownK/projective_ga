implementing rga3d;

/// Bulk
/// This characterizes the flat aspect's relationship with the origin.
public static const BulkPrefixOrPostfix bulk = BulkPrefixOrPostfix();
public struct BulkPrefixOrPostfix {}
__include "./impls/bulk";
